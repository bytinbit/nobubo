class PdfProperties:
    def __init__(self, rows, cols, x_offset, y_offset, pages):
        self.ROWS = rows
        self.COLS = cols
        self.X_OFFSET = x_offset
        self.Y_OFFSET = y_offset
        self.number_of_pages = pages
